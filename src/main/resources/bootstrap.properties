spring.application.name=transfer-module
server.port=8083

greeting = greeting from transfer default application properties file







management.endpoints.web.exposure.include=*

testValue = test

app.name = transfer-module
app.discription = this is transfer module application
app.greeting = ${greeting}

my.list.values = one,two,three

dbValues = {url: 'xyz', username: 'nikhil', password: 'nikhil'}

spring.profiles.active=prod
spring.cloud.config.uri=http://localhost:9090
